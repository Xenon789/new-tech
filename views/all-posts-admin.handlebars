<div class="posts">
    {{#each posts}}
        <a href="/dashboard/edit/{{id}}" class="list-item">
            <span>{{title}}</span>
            <span>{{format_date createdAt}}</span>
        </a>
    {{/each}}
</div>